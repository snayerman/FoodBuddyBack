language: node_js
sudo: true
node_js:
  - "iojs"
  - "7"

before_install:
  - '[ "${TRAVIS_NODE_VERSION}" != "0.8" ] || npm install -g npm@6'
  - npm install -g npm@latest

script:
  - npm start

cache:
  directories:
    - "node__modules"