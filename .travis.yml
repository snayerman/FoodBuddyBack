language: node_js
sudo: true
node_js:
  - "iojs"
  - "7"

before_install:
  - '[ "${TRAVIS_NODE_VERSION}" != "0.8" ] || npm install -g npm@1.4.28'
  - npm install -g npm@latest

script:
  - npm start

cache:
  directories:
    - "node__modules"